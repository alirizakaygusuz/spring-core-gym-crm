spring:

  data:
    mongodb:
      username: ${DEV_MONGO_USER}
      password: ${DEV_MONGO_PASSWORD}
      host: ${DEV_MONGO_HOST}
      port: ${DEV_MONGO_PORT}
      database: ${DEV_MONGO_DB}

  artemis:
    broker-url: ${DEV_ARTEMIS_BROKER_URL}
    user: ${DEV_ARTEMIS_USER}
    password: ${DEV_ARTEMIS_PASSWORD}

logging:
  level:
    com.alirizakaygusuz.gymcrm.workload_service: DEBUG